# Frontend — variáveis de ambiente
# Copie para .env.local e preencha antes de rodar

# URL do backend (em dev: http://localhost:8000)
VITE_API_URL=http://localhost:8000

# Supabase (anon key — pode ser exposta no frontend)
VITE_SUPABASE_URL=https://dzrigbptvlpbruxtkbuu.supabase.co
VITE_SUPABASE_ANON_KEY=
