# =============================================================================
# .env.example — commitar este arquivo. NUNCA commitar o .env real.
# Copie para .env e preencha os valores antes de rodar o projeto.
# =============================================================================

# --- Supabase ---
SUPABASE_URL=https://dzrigbptvlpbruxtkbuu.supabase.co
SUPABASE_SERVICE_KEY=           # service role key — NUNCA expor no frontend (Dashboard → Settings → API)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImR6cmlnYnB0dmxwYnJ1eHRrYnV1Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzUwMDc3MjEsImV4cCI6MjA5MDU4MzcyMX0.N5bbIWIOsV53hrWI86100QzW7pddtnrHleytBsvrwHU

# --- Banco (direto via SQLAlchemy no backend) ---
DATABASE_URL=postgresql+asyncpg://postgres:[SUA_SENHA_DB]@db.dzrigbptvlpbruxtkbuu.supabase.co:5432/postgres

# --- Auth JWT ---
JWT_SECRET_KEY=                 # mínimo 64 chars — gerar: python -c "import secrets; print(secrets.token_hex(32))"
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# --- Biometria (LGPD) ---
FACIAL_ENCRYPTION_KEY=          # AES-256 base64 de 32 bytes — gerar: python -c "import secrets,base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
FACIAL_SIMILARITY_THRESHOLD=0.75

# --- NTP (exigência Portaria 671) ---
NTP_SERVER=a.ntp.br

# --- Redis (Celery + cache) ---
REDIS_URL=redis://localhost:6379/0

# --- Monitoramento ---
SENTRY_DSN=

# --- App ---
ENVIRONMENT=development         # development | staging | production
LOG_LEVEL=INFO
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8788,https://ponto.empresa.com.br

# --- Cloudflare (frontend Workers) ---
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
